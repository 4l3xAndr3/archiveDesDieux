package ui.theme

import androidx.compose.ui.graphics.Color

// Couleurs de rareté
val RarityCommon = Color(0xFF4A90E2) // Bleu
val RarityRare = Color(0xFF9B59B6) // Violet
val RarityLegendary = Color(0xFFFFD700) // Or

// Couleurs de factions
val FactionGrec = Color(0xFFE8D4A0) // Or crème (Olympe)
val FactionEgyptien = Color(0xFFD4A574) // Or ocre (Désert)
val FactionNordique = Color(0xFF7FB3D5) // Bleu glacé (Glace)

// Couleurs de base
val DarkStone = Color(0xFF1A1A1A)
val LightStone = Color(0xFF2D2D2D)
val GlowWhite = Color(0xFFE0E0E0)
val GlowBlue = Color(0xFF00D4FF)

// Effets légendaires
val EffectGrec = Color(0xFFFFE5B4) // Ambroisie dorée
val EffectNordique = Color(0xFFB0E0E6) // Blizzard bleu
val EffectEgyptien = Color(0xFFDAA520) // Tempête de sable
